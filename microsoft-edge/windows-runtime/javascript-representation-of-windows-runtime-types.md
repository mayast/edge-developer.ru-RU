---
title: Представление типов среды выполнения Windows в JavaScript
ms.custom: ''
ms.date: 07/29/2020
ms.prod: microsoft-edge
ms.reviewer: ''
ms.suite: ''
ms.technology: windows-integration
ms.tgt_pltfrm: ''
ms.topic: article
helpviewer_keywords:
- Windows Runtime types [JavaScript]
- JavaScript, Windows Runtime types
ms.assetid: f4851802-8b40-4afa-ba6c-8a162a9a43cc
caps.latest.revision: 9
author: MSEdgeTeam
ms.author: msedgedevrel
manager: ''
ms.openlocfilehash: 0ee55b5dd5071b0f0b31656ae164e9801e98d6ac
ms.sourcegitcommit: 29cbe0f464ba0092e025f502833eb9cc3e02ee89
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 08/20/2020
ms.locfileid: "10942050"
---
# <span data-ttu-id="1da17-102">Представление типов среды выполнения Windows в JavaScript</span><span class="sxs-lookup"><span data-stu-id="1da17-102">JavaScript representation of Windows Runtime types</span></span>  

[!INCLUDE [deprecation-note](../includes/legacy-edge-note.md)]  

<span data-ttu-id="1da17-103">В приведенной ниже таблице описано, как JavaScript представляет типы выполнения Windows Runtime.</span><span class="sxs-lookup"><span data-stu-id="1da17-103">The following table describes the way JavaScript represents Windows Runtime types.</span></span>  

> [!IMPORTANT]
> <span data-ttu-id="1da17-104">Функции выполнения Windows недоступны для приложений, работающих в Internet Explorer.</span><span class="sxs-lookup"><span data-stu-id="1da17-104">Windows Runtime features are not available for apps that run in Internet Explorer.</span></span>  

## <span data-ttu-id="1da17-105">Типы выполнения Windows в JavaScript</span><span class="sxs-lookup"><span data-stu-id="1da17-105">Windows Runtime types in JavaScript</span></span>  

| <span data-ttu-id="1da17-106">Тип Windows Runtime</span><span class="sxs-lookup"><span data-stu-id="1da17-106">Windows Runtime type</span></span> | <span data-ttu-id="1da17-107">Представление JavaScript</span><span class="sxs-lookup"><span data-stu-id="1da17-107">JavaScript representation</span></span> | <span data-ttu-id="1da17-108">Описание</span><span class="sxs-lookup"><span data-stu-id="1da17-108">Description</span></span> |  
|:--- |:--- |:--- |  
| `UInt8` | `Number` | <span data-ttu-id="1da17-109">Windows Runtime — `Uint8` это 8-разрядное целое число без знака с неподписанным целым числом, представленным как JavaScript number.</span><span class="sxs-lookup"><span data-stu-id="1da17-109">A Windows Runtime `Uint8` is an unsigned 8-bit integer, represented as a JavaScript number.</span></span>  <span data-ttu-id="1da17-110">Значение JavaScript сначала преобразуется в номер JavaScript, после чего `ToUint32` выполняется процесс.</span><span class="sxs-lookup"><span data-stu-id="1da17-110">A JavaScript value is first converted to a JavaScript number, and then the `ToUint32` process is followed.</span></span>  <span data-ttu-id="1da17-111">Если значение номера JavaScript находится за пределами диапазона Uint8, в результате будет применен модуль 2^8.</span><span class="sxs-lookup"><span data-stu-id="1da17-111">If the JavaScript number value is outside the range of a Uint8, the result will have modulo 2^8 applied.</span></span>  |  
| `Int32` | `Number` | <span data-ttu-id="1da17-112">Во время выполнения Windows `Int32` служит 32-разрядное целое число со знаком с номером JavaScript.</span><span class="sxs-lookup"><span data-stu-id="1da17-112">A Windows Runtime `Int32` is a signed 32-bit integer, represented as a JavaScript number.</span></span>  <span data-ttu-id="1da17-113">Значение JavaScript сначала преобразуется в номер JavaScript, после чего `ToInt32` выполняется процесс.</span><span class="sxs-lookup"><span data-stu-id="1da17-113">A JavaScript value is first converted to a JavaScript number, and then the `ToInt32` process is followed.</span></span>  <span data-ttu-id="1da17-114">Если значение номера JavaScript находится за пределами `Int32` диапазона, будет применен модуль 2^16.</span><span class="sxs-lookup"><span data-stu-id="1da17-114">If the JavaScript number value is outside the range of an `Int32`, the result will have modulo 2^16 applied.</span></span>  |  
| `Int64` | `Number` | <span data-ttu-id="1da17-115">`Int64`64-разрядное целое число с знаком "64-разрядная", представляемое как стандартное число, если он попадется в диапазон \[-2^53\].</span><span class="sxs-lookup"><span data-stu-id="1da17-115">A Windows Runtime `Int64` is a signed 64-bit integer, represented as a standard number if it falls within the range \[-2^53, 2^53\].</span></span>  <span data-ttu-id="1da17-116">Если она выходит за этот диапазон, оно представляется как числовое значение, которое в котором сохраняется полный хранилище для шести целых чисел.</span><span class="sxs-lookup"><span data-stu-id="1da17-116">If it falls outside this range, it is represented as a number value that has a custom backing store that maintains the full 64 bits of integer data.</span></span>  <span data-ttu-id="1da17-117">Все математические операции с этими числовыми номерами Int64 приводит к преобразованию его в стандартное число в диапазоне \[-2^53, 2^53\].</span><span class="sxs-lookup"><span data-stu-id="1da17-117">All mathematical operations on these custom Int64 number values cause the value to be converted to a standard number in the range \[-2^53, 2^53\].</span></span>  <span data-ttu-id="1da17-118">Если значение находится за пределами этого диапазона, возникает ошибка типа.</span><span class="sxs-lookup"><span data-stu-id="1da17-118">If the value is outside this range, a type error is raised.</span></span>  <span data-ttu-id="1da17-119">Исключения из этого процесса `==` преобразования: `===` " ", `SameValue` ", `RelationalComparison` " и операции, которые сравнивают аргументы с учетом полных 64-битов при переносе двух битов, представленных в 64-разрядной.</span><span class="sxs-lookup"><span data-stu-id="1da17-119">The exceptions to this conversion process are the `==`, `===`, `SameValue`, and `RelationalComparison` operations, which compare the arguments based on the full 64 bits when passed two represented 64-bit values.</span></span>  <span data-ttu-id="1da17-120">Если значение какого-либо из аргументов является стандартным номером JavaScript, эти операции также преобразуются в номер JavaScript перед сравнением.</span><span class="sxs-lookup"><span data-stu-id="1da17-120">If either argument is a standard JavaScript number, these operations also convert the argument to a JavaScript number before comparison.</span></span>  <span data-ttu-id="1da17-121">Значение JavaScript назначается непосредственно, если это Int64 само значение. В противном итоге применение преобразования значения `ToInteger` в среду среды выполнения Windows.</span><span class="sxs-lookup"><span data-stu-id="1da17-121">A JavaScript value is directly assigned if it is an Int64 value itself; otherwise the result of applying the `ToInteger` conversion on the value is passed to Windows Runtime.</span></span>  <span data-ttu-id="1da17-122">Если возникнет сбой согласия типов, возвращается исключение.</span><span class="sxs-lookup"><span data-stu-id="1da17-122">If the type coercion fails, an exception is returned.</span></span>  |  
| `Uint64` | `Number` | <span data-ttu-id="1da17-123">`Uint64`64-разрядное целое число без знака и представляет собой стандартное число, если оно попадет в диапазон \[0, 2^53\].</span><span class="sxs-lookup"><span data-stu-id="1da17-123">A Windows Runtime `Uint64` is an unsigned 64-bit integer, represented as a standard number if it falls within the range \[0, 2^53\].</span></span>  <span data-ttu-id="1da17-124">Если она выходит за этот диапазон, он будет иметь число с настраиваемым магазином для обратной связи, в котором будут храниться полные 64 биты неподписанных целых чисел без знака.</span><span class="sxs-lookup"><span data-stu-id="1da17-124">If it falls outside this range, it is represented as a number that has a custom backing store that maintains the full 64 bits of unsigned integer data.</span></span>  <span data-ttu-id="1da17-125">Все математические операции на этих пользовательских значениях Uint64 приводит к преобразованию его в стандартное число в диапазоне \[0, 2^53\].</span><span class="sxs-lookup"><span data-stu-id="1da17-125">All mathematical operations on these custom Uint64 values cause the value to be converted to a standard number in the range \[0, 2^53\].</span></span>  <span data-ttu-id="1da17-126">Если значение находится за пределами этого диапазона, возникает ошибка типа.</span><span class="sxs-lookup"><span data-stu-id="1da17-126">If the value is outside this range, a type error is raised.</span></span>  <span data-ttu-id="1da17-127">Исключения из этого процесса `==` преобразования: `===` , , , `SameValue` и `RelationalComparison` операции, которые сравнивают аргументы с учетом полных 64-разрядных значений при переносе два-разрядных значения.</span><span class="sxs-lookup"><span data-stu-id="1da17-127">The exceptions to this conversion process are the `==`, `===`, `SameValue`, and `RelationalComparison` operations, which compare the arguments based on the full 64 bits when passed two 64-bit values.</span></span>  <span data-ttu-id="1da17-128">Если значение какого-либо из аргументов является стандартным номером JavaScript, эти операции также преобразуются в номер JavaScript перед сравнением.</span><span class="sxs-lookup"><span data-stu-id="1da17-128">If either argument is a standard JavaScript number, these operations also convert the argument to a JavaScript number before comparison.</span></span>  <span data-ttu-id="1da17-129">Значение JavaScript назначается непосредственно, если оно является само значением Uint64. В противном случае результат применения преобразования значения, за которым `ToInteger` путаница 2^52, пройдет проверка windows в среду выполнения Windows.</span><span class="sxs-lookup"><span data-stu-id="1da17-129">A JavaScript value is directly assigned if it is a Uint64 value itself; otherwise, the result of applying the `ToInteger` conversion on the value, followed by taking modulo 2^52, is passed to Windows Runtime.</span></span>  <span data-ttu-id="1da17-130">Если преобразование типа не произойдет, возвращается исключение.</span><span class="sxs-lookup"><span data-stu-id="1da17-130">If the type conversion fails, an exception is returned.</span></span>  |  
| `Single` | `Number` | <span data-ttu-id="1da17-131">Windows Runtime `Single` — это 32-разрядное число с плавающей запятой, которое представлено как номер JavaScript, выбрав ближайшее значение точности для одной точности.</span><span class="sxs-lookup"><span data-stu-id="1da17-131">A Windows Runtime `Single` is a 32-bit floating-point number, represented as a JavaScript number by picking the closest double-precision value to the single-precision value.</span></span>  <span data-ttu-id="1da17-132">Значение JavaScript преобразуется в номер JavaScript, а затем — установленный флажок, чтобы значение было представлено в 32-разрядной версии IEEE 754 с плавающей запятой.</span><span class="sxs-lookup"><span data-stu-id="1da17-132">A JavaScript value is converted to a JavaScript number and then range checked to ensure that the value can be represented in a single-precision 32-bit IEEE 754 floating-point number.</span></span>  <span data-ttu-id="1da17-133">Если проверка или диапазон не пройдена, возвращается ошибка поля.</span><span class="sxs-lookup"><span data-stu-id="1da17-133">If the conversion or range check fails, a marshaling error is returned.</span></span>  |  
| `Double` | `Number` | <span data-ttu-id="1da17-134">Во время выполнения Windows `Double` выбирается 64-разрядное число с плавающей запятой.</span><span class="sxs-lookup"><span data-stu-id="1da17-134">A Windows Runtime `Double` is a 64-bit floating-point number.</span></span>  `ToNumber` <span data-ttu-id="1da17-135">используется для преобразования кода Выполнения Windows Runtime `Double` в номер JavaScript.</span><span class="sxs-lookup"><span data-stu-id="1da17-135">is used to convert a Windows Runtime `Double` to a JavaScript number.</span></span>  <span data-ttu-id="1da17-136">Если преобразование завершится сбоем, возвращается ошибка маршрутизации.</span><span class="sxs-lookup"><span data-stu-id="1da17-136">If the conversion fails, a marshaling error is returned.</span></span>  |  
| `Boolean` | `Boolean` | <span data-ttu-id="1da17-137">Выполнение Windows — это 8-битное значение, где нулевым совпадает и любое значение, отличный `Boolean` `false` от нуля, представлено в виде `true` JavaScript. `Boolean`</span><span class="sxs-lookup"><span data-stu-id="1da17-137">A Windows Runtime `Boolean` is an 8-bit value where zero is `false` and any value other than zero is `true`, represented as a JavaScript `Boolean`.</span></span>  <span data-ttu-id="1da17-138">Значение JavaScript сначала преобразуется в JavaScript. `Boolean` `ToBoolean`</span><span class="sxs-lookup"><span data-stu-id="1da17-138">A JavaScript value is first converted to a JavaScript `Boolean` by `ToBoolean`.</span></span>  <span data-ttu-id="1da17-139">Это означает, что функция среды выполнения Windows обрасплевена так, как можно записывать `void func(BOOL);` в JavaScript `obj.func("test");` как.</span><span class="sxs-lookup"><span data-stu-id="1da17-139">This means that a Windows Runtime function declared as `void func(BOOL);` could be written in JavaScript as `obj.func("test");`.</span></span>  <span data-ttu-id="1da17-140">Функция Windows Runtime называется `BOOL` параметром, который был переведен как параметр, который был переведен `TRUE` как параметр.</span><span class="sxs-lookup"><span data-stu-id="1da17-140">The Windows Runtime function is called with the `BOOL` parameter passed as `TRUE`.</span></span>  <span data-ttu-id="1da17-141">Если преобразование завершится сбоем, возвращается ошибка маршрутизации.</span><span class="sxs-lookup"><span data-stu-id="1da17-141">If the conversion fails, a marshaling error is returned.</span></span>  |  
| `Char16` | `String` | <span data-ttu-id="1da17-142">Среда выполнения Windows — `Char16` это 16-битный символ Юникода, содержащий один символ.</span><span class="sxs-lookup"><span data-stu-id="1da17-142">A Windows Runtime `Char16` is a 16-bit Unicode character, represented as a JavaScript string that contains a single character.</span></span>  <span data-ttu-id="1da17-143">Значение JavaScript преобразуется в строку JavaScript, проверяя, надоличить у достижения длиной `ToString` единого символа.</span><span class="sxs-lookup"><span data-stu-id="1da17-143">A JavaScript value is converted to a JavaScript string by `ToString` and then checked to ensure that the string is only a single character long.</span></span>  <span data-ttu-id="1da17-144">После этого в качестве значения будет добавлен один `Char16` символ.</span><span class="sxs-lookup"><span data-stu-id="1da17-144">The single character is then passed as the `Char16` value.</span></span>  <span data-ttu-id="1da17-145">Если проверка или длина проверки завершится сбоем, возвращается ошибка "Разное поле".</span><span class="sxs-lookup"><span data-stu-id="1da17-145">If the conversion or length check fails, a marshaling error is returned.</span></span>  |  
| `String` | `String` | <span data-ttu-id="1da17-146">Windows Runtime представляет собой немного важную последовательность `String` `Char16` объектов.</span><span class="sxs-lookup"><span data-stu-id="1da17-146">A Windows Runtime `String` is an immutable sequence of `Char16` objects.</span></span>  <span data-ttu-id="1da17-147">Строки представлены как объекты JavaScript. `String`</span><span class="sxs-lookup"><span data-stu-id="1da17-147">Strings are represented as JavaScript `String` objects.</span></span>  <span data-ttu-id="1da17-148">Для строк среды выполнения Windows не требуются разные значения для и пустой `null` строки \("\).</span><span class="sxs-lookup"><span data-stu-id="1da17-148">Windows Runtime strings do not have different values for `null` and empty string \(""\).</span></span>  `null` <span data-ttu-id="1da17-149">пустые строковые значения преобразуются в пустую строку JavaScript пустой строки JavaScript.</span><span class="sxs-lookup"><span data-stu-id="1da17-149">and empty string values are converted to the JavaScript empty string.</span></span>  <span data-ttu-id="1da17-150">**ПРИМЕЧАНИЕ.** Когда JavaScript присутствует `null` значение JavaScript, ожидающее строку, возвращается строковое значение "Null", а не `null` пустая строка \("\).</span><span class="sxs-lookup"><span data-stu-id="1da17-150">**NOTE**:  When JavaScript `null` is passed to a Windows Runtime parameter that expects a string, it produces the string value "null", not `null` or the empty string \(""\).</span></span>  <span data-ttu-id="1da17-151">Строки, которые были переданныы в среду выполнения Windows Runtime, всегда должны быть перечислены в пустую строку.</span><span class="sxs-lookup"><span data-stu-id="1da17-151">Strings passed to the Windows Runtime should always be instantiated to the empty string.</span></span>  |  
| `Enumeration` | `Object` | <span data-ttu-id="1da17-152">Среда выполнения Windows — `Enumeration` это 32-битное целое число \(подписанная или неподписанная\), которое имеет связанный набор именованных констант.</span><span class="sxs-lookup"><span data-stu-id="1da17-152">A Windows Runtime `Enumeration` is a 32-bit integer \(signed or unsigned\) that has an associated set of named constants.</span></span>  <span data-ttu-id="1da17-153">В JavaScript перечисление представлены в виде объекта, содержащего поле с доступом только для чтения для чтения.</span><span class="sxs-lookup"><span data-stu-id="1da17-153">In JavaScript, an enumeration is represented as an object that contains a read-only field for each named value.</span></span>  <span data-ttu-id="1da17-154">Значения перечисления преобразуются в номера JavaScript, определенные ранее для `Int32` и сохранения. `UInt32`</span><span class="sxs-lookup"><span data-stu-id="1da17-154">Enumeration values are converted to JavaScript numbers as defined earlier for `Int32` and `UInt32`.</span></span>  <span data-ttu-id="1da17-155">При преобразовании значения JavaScript в перечисления среды выполнения это значение преобразуется, усечено и диапазоном, как описано выше.</span><span class="sxs-lookup"><span data-stu-id="1da17-155">When a JavaScript value is converted to a Windows Runtime enumeration, it is converted, truncated and range checked as described earlier.</span></span>  <span data-ttu-id="1da17-156">Однако полученное значение не проверяется на заданные именованные значения, указанные в перечислении.</span><span class="sxs-lookup"><span data-stu-id="1da17-156">However, the resulting value is not checked against the defined named values that are specified in the enumeration.</span></span>  |  
| `Structure` | `Object` | <span data-ttu-id="1da17-157">Windows Runtime — `Structure` это шестеренку именованных полей данных.</span><span class="sxs-lookup"><span data-stu-id="1da17-157">A Windows Runtime `Structure` is a heterogeneous collection of named data fields.</span></span>  <span data-ttu-id="1da17-158">В JavaScript структура представлена как объект JavaScript, содержащий именованное свойство каждого поля в структуре.</span><span class="sxs-lookup"><span data-stu-id="1da17-158">In JavaScript, a structure is represented as a JavaScript object that contains a named property for each field in the structure.</span></span>  <span data-ttu-id="1da17-159">В случае сбоя структуры любого значения возвращается ошибка при ошибке изменения.</span><span class="sxs-lookup"><span data-stu-id="1da17-159">If the conversion of any structure value fails, a marshaling error is returned.</span></span>  <span data-ttu-id="1da17-160">Поля объекта JavaScript, у которых нет эквивалентных данных в структуре выполнения Windows, игнорируются.</span><span class="sxs-lookup"><span data-stu-id="1da17-160">Fields in the JavaScript object that do not have equivalents in the Windows Runtime structure are ignored.</span></span>  <span data-ttu-id="1da17-161">**ПРИМЕЧАНИЕ.** Структуру выполнения Windows Runtime невозможно справиться с ключевым `new` словом JavaScript.</span><span class="sxs-lookup"><span data-stu-id="1da17-161">**NOTE**:  A Windows Runtime structure cannot be instantiated in JavaScript with the `new` keyword.</span></span>  |  
| `Array` | `Array` | <span data-ttu-id="1da17-162">Во время выполнения Windows `Array` выполняется преобразование в массив JavaScript.</span><span class="sxs-lookup"><span data-stu-id="1da17-162">A Windows Runtime `Array` is converted to a JavaScript array.</span></span>  <span data-ttu-id="1da17-163">Однако размер массивов Windows Runtime невозможно изменить, поэтому некоторые операции массива JavaScript недоступны.</span><span class="sxs-lookup"><span data-stu-id="1da17-163">However, Windows Runtime arrays are not resizable, so some JavaScript array operations are not possible.</span></span>  <span data-ttu-id="1da17-164">Внутреннее `[[Class]]` свойство преобразованного массива не равно "массиву", поскольку это действие не допустило спецификацию ECMAScript 5.</span><span class="sxs-lookup"><span data-stu-id="1da17-164">The internal `[[Class]]` property of a converted array is not set to "Array" because this is not allowed by the ECMAScript 5 specification.</span></span>  <span data-ttu-id="1da17-165">Это означает, что `Array.isArray(v)` возвращается `false` значение.</span><span class="sxs-lookup"><span data-stu-id="1da17-165">This means that `Array.isArray(v)` returns `false`.</span></span>  <span data-ttu-id="1da17-166">Значение JavaScript преобразуется в массив выполнения Windows следующим образом: если значение имеет значение или оно преобразуется в `null` `undefined` собственный. `null`</span><span class="sxs-lookup"><span data-stu-id="1da17-166">A JavaScript value is converted to a Windows Runtime array as follows:  If the value is `null` or `undefined`, it is converted to a native `null`.</span></span>  <span data-ttu-id="1da17-167">Если его внутреннее свойство скопировано в массив, то оно копируется в массив, а ссылка на `[[Class]]` этот массив пройдет.</span><span class="sxs-lookup"><span data-stu-id="1da17-167">If its internal `[[Class]]` property is "Array", it is copied to a native array and a reference to this array is passed.</span></span>  <span data-ttu-id="1da17-168">Если это преобразованный массив, как описано выше, в качестве встроенного массива производится собственный массив.</span><span class="sxs-lookup"><span data-stu-id="1da17-168">If it is a converted array, as described previously, the underlying native array is passed.</span></span>  <span data-ttu-id="1da17-169">**ПРИМЕЧАНИЕ.** При принципе к методу выполнения Windows Runtime приводит к полной копии массива.</span><span class="sxs-lookup"><span data-stu-id="1da17-169">**NOTE**:  Passing a JavaScript array value to a Windows Runtime method causes a full copy of the array.</span></span>  |  
| <span data-ttu-id="1da17-170">Delegate \(функция callback\)</span><span class="sxs-lookup"><span data-stu-id="1da17-170">Delegate \(function callback\)</span></span> | `Function` | <span data-ttu-id="1da17-171">Делегат выполнения Windows — это ссылка на один метод.</span><span class="sxs-lookup"><span data-stu-id="1da17-171">A Windows Runtime delegate is a reference to a single method.</span></span>  <span data-ttu-id="1da17-172">Делегат выполнения Windows Runtime представленва в виде JavaScript, который гарантированно вызывается в `Function` нужной цепочке.</span><span class="sxs-lookup"><span data-stu-id="1da17-172">A Windows Runtime delegate is represented in JavaScript as a JavaScript `Function`, which is guaranteed to be called on the proper thread.</span></span>  <span data-ttu-id="1da17-173">При `Function` вызове аргументы преобразуются в аналогичные типы параметров Windows.</span><span class="sxs-lookup"><span data-stu-id="1da17-173">When the `Function` is invoked, the arguments are converted to the equivalent Windows Runtime parameter types.</span></span>  <span data-ttu-id="1da17-174">Если аргументов JavaScript меньше, чем параметры `in` делегирования, делегирование не работает.</span><span class="sxs-lookup"><span data-stu-id="1da17-174">If there are fewer JavaScript arguments than delegate `in` parameters, the delegate call fails.</span></span>  <span data-ttu-id="1da17-175">Если представителю были исключены другие аргументы JavaScript, чем для представителя, `in` аргументы дополнительных аргументов JavaScript игнорируются.</span><span class="sxs-lookup"><span data-stu-id="1da17-175">If there are more JavaScript arguments than there are `in` parameters in the delegate, the extra JavaScript arguments are ignored.</span></span>  <span data-ttu-id="1da17-176">После вызова представителя параметры преобразуются `out` в типы JavaScript и вернутся.</span><span class="sxs-lookup"><span data-stu-id="1da17-176">After the delegate is invoked, the `out` parameters are converted to JavaScript types and returned.</span></span>  <span data-ttu-id="1da17-177">При преобразовании объекта функции JavaScript в делегат среды выполнения Windows переносится в делегат на Windows Runtime соответствующего типа.</span><span class="sxs-lookup"><span data-stu-id="1da17-177">If a native JavaScript function object is converted to a Windows Runtime delegate, it is wrapped in a Windows Runtime delegate of the corresponding type.</span></span>  <span data-ttu-id="1da17-178">При вызове представителя параметры преобразуются `in` в типы JavaScript.</span><span class="sxs-lookup"><span data-stu-id="1da17-178">When the delegate is invoked, the `in` parameters are converted to JavaScript types.</span></span>  <span data-ttu-id="1da17-179">После вызова представителя возвращаемое значение преобразуется в `out` параметры делегата.</span><span class="sxs-lookup"><span data-stu-id="1da17-179">After the delegate is invoked, the return value is converted to the `out` parameters of the delegate.</span></span>  |  
| <span data-ttu-id="1da17-180">Интерфейс</span><span class="sxs-lookup"><span data-stu-id="1da17-180">Interface</span></span> | `Object` | <span data-ttu-id="1da17-181">Группы интерфейсов и интерфейсов не представлены непосредственно в JavaScript как объекты.</span><span class="sxs-lookup"><span data-stu-id="1da17-181">Interfaces and interface groups are not directly represented in JavaScript as objects.</span></span>  <span data-ttu-id="1da17-182">Однако интерфейсы представлены в виде параметра и возвращаемых методов Windows Runtime.</span><span class="sxs-lookup"><span data-stu-id="1da17-182">However, interfaces are represented as parameter and return types of Windows Runtime methods.</span></span>  <span data-ttu-id="1da17-183">Экземпляр интерфейса Windows преобразуется в следующий синтаксис:</span><span class="sxs-lookup"><span data-stu-id="1da17-183">A Windows Runtime interface instance is converted as follows:</span></span><br /> <br /> <span data-ttu-id="1da17-184">1. Если имеется допустимое занятие выполнения, представьте его как экземпляр этого класса.</span><span class="sxs-lookup"><span data-stu-id="1da17-184">1.  If there is a valid runtime class, represent the object as an instance of that class.</span></span><br /> <span data-ttu-id="1da17-185">2. Если нет допустимого класса среды выполнения, представьте объект в качестве экземпляра неименованного занятия среды выполнения, который реализует интерфейс того, что экземпляр известен для реализации и необходимых интерфейсов.</span><span class="sxs-lookup"><span data-stu-id="1da17-185">2.  If there is no valid runtime class, represent the object as an instance of an unnamed runtime class that implements exactly the interface that the instance is known to implement plus its required interfaces.</span></span><br /> <br /> <span data-ttu-id="1da17-186">Значение JavaScript проверяется, чтобы определить, является ли он экземпляром класса среды выполнения или интерфейса выполнения.</span><span class="sxs-lookup"><span data-stu-id="1da17-186">A JavaScript value is tested to determine whether it is an instance of a runtime class or an interface.</span></span>  <span data-ttu-id="1da17-187">Если это так, и исходное значение среды выполнения Windows реализовано в интерфейсе, он переносится в среду выполнения Windows Runtime.</span><span class="sxs-lookup"><span data-stu-id="1da17-187">If it is, and the original Windows Runtime value implements the interface, the object is passed to the Windows Runtime.</span></span>  |  
| <span data-ttu-id="1da17-188">Классы выполнения</span><span class="sxs-lookup"><span data-stu-id="1da17-188">Runtime classes</span></span> | `Object` | <span data-ttu-id="1da17-189">Объекты в среде среды выполнения Windows могут быть экземплярами классов среды выполнения, которые реализуют один или несколько интерфейсов.</span><span class="sxs-lookup"><span data-stu-id="1da17-189">Objects in the Windows Runtime can be instances of runtime classes that implement one or more interfaces.</span></span>  <span data-ttu-id="1da17-190">Объекты PowerPoint представлены в виде объектов JavaScript.</span><span class="sxs-lookup"><span data-stu-id="1da17-190">Windows Runtime objects are represented in JavaScript as objects.</span></span>  <span data-ttu-id="1da17-191">Объединение методов, свойств и событий, определенных во всех реализованных интерфейсах класса, обозначает имена свойств объекта JavaScript.</span><span class="sxs-lookup"><span data-stu-id="1da17-191">The union of methods, properties, and events defined on all the implemented interfaces of the class represents the named properties in the prototype of the JavaScript object.</span></span>  <span data-ttu-id="1da17-192">Потребители JavaScript могут получить доступ непосредственно к любым участникам класса.</span><span class="sxs-lookup"><span data-stu-id="1da17-192">JavaScript consumers can access any member of the class directly.</span></span>  <span data-ttu-id="1da17-193">Объекты Windows имеет прототип, содержащий все хотя бы одного из внедренных интерфейсов класса среды выполнения.</span><span class="sxs-lookup"><span data-stu-id="1da17-193">Windows Runtime objects have a prototype that contains all the members from all the implemented interfaces of the runtime class.</span></span>  |  
  
## <span data-ttu-id="1da17-194">См. также</span><span class="sxs-lookup"><span data-stu-id="1da17-194">See also</span></span>  

[<span data-ttu-id="1da17-195">Использование среды выполнения Windows в JavaScript</span><span class="sxs-lookup"><span data-stu-id="1da17-195">Using the Windows Runtime in JavaScript</span></span>][WindowsRuntimeJavascript]  

<!-- links -->  

[WindowsRuntimeJavascript]: ./using-the-windows-runtime-in-javascript.md "Использование Windows Runtime в JavaScript | Документы Майкрософт"  
